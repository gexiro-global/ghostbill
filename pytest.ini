[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    service: Service-level tests calling production code directly
    integration: HTTP-level tests against live backend
    slow: Tests that take >5s
    stress: Load/stress tests
    destructive: Tests that modify shared state
